name: links
description: "A link-in-bio website built with Flutter"
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.5.0-99.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
